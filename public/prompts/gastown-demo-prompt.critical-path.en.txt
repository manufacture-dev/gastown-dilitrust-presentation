I want to run a lightweight Agreement Hub demo focused only on the critical path.

Do not start broad autonomous delivery. Do not create product convoys unless they are strictly needed for the critical path.

First, inspect the repository enough to understand the existing app, docs, tests, and conventions.

Then implement the smallest verified path that proves the demo:
- a user can see the relevant Agreement Hub workflow;
- the critical action can be completed end to end;
- the result is visible in the UI;
- the app still works in mock/offline mode without LLM access.

Make reasonable architecture, naming, file ownership, and sequencing decisions yourself using existing project patterns.

Avoid parallel work on shared integration files. If foundation work is required, finish and verify it before dependent changes.

Before claiming completion, verify directly on the delivery branch or current working branch:
- the critical workflow is present;
- frontend and backend checks pass, if they exist;
- mock/offline demo behavior works;
- no copied prompt or UI text contains HTML artifacts.

Report only:
- shipped scope;
- files changed;
- verification evidence;
- remaining risks or deferred non-critical work.
