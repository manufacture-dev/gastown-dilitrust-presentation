Je veux lancer une demo Agreement Hub legere, centree uniquement sur le chemin critique.

Ne lance pas de delivery autonome large. Ne cree pas de convoys produit sauf s'ils sont strictement necessaires au chemin critique.

D'abord, inspecte le depot juste assez pour comprendre l'application existante, la documentation, les tests et les conventions.

Ensuite, implemente le plus petit chemin verifie qui prouve la demo:
- un utilisateur voit le workflow Agreement Hub pertinent;
- l'action critique peut etre terminee de bout en bout;
- le resultat est visible dans l'UI;
- l'application fonctionne encore en mode mock/offline sans acces LLM.

Prends toi-meme les decisions raisonnables d'architecture, nommage, ownership des fichiers et sequencing en suivant les patterns existants.

Evite le travail parallele sur les fichiers d'integration partages. Si un travail de fondation est necessaire, termine-le et verifie-le avant les changements dependants.

Avant d'annoncer la completion, verifie directement sur la branche de delivery ou la branche de travail courante:
- le workflow critique est present;
- les checks frontend et backend passent, s'ils existent;
- le mode mock/offline de demo fonctionne;
- aucun prompt copie ni texte UI ne contient d'artefacts HTML.

Reporte seulement:
- le scope livre;
- les fichiers modifies;
- les preuves de verification;
- les risques restants ou le travail non critique differe.
